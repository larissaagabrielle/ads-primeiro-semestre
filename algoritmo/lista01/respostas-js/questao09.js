let base = prompt ("Informe a base do retângulo:")
let altura = prompt ("Informe a altura do do retângulo:")

console.log(`A área do retângulo é ${base * altura}`);

console.log(`O perímetro do retângulo é ${(base * 2) + (altura * 2)}`);

document.write(`A área do retângulo é ${base * altura} <br>`);
document.write(`O perímetro do retângulo é ${(base * 2) + (altura * 2)}`);document.write